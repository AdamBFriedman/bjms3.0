import { createTheme } from "@material-ui/core/styles";
const bjmsTheme = createTheme({
  palette: {
    primary: { main: "#820CCB", dark: "#426fff" },
    secondary: { main: "#2FCB00", light: "#f8ee03" },
    grey: { main: "#bdc3c7" },
  },
});
export default bjmsTheme;
